package test.util;
/*
import org.junit.Test;
import util.promise.Promise;

public class PromiseTest {
    @Test
    public void promiseTest() {
        Promise<String> promise = Promise.async(o -> "It works! (Data: " + o + ")");
        String string = Promise.await(promise, "Test");
        System.out.println(string);
    }

    @Test
    public void promiseHeavyTask() {
        Promise<String> promise = Promise.async(o -> {
            try {
                Thread.sleep(5000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            return "It works! (Data: " + o + ")";
        });
        String string = Promise.await(promise, "Test");
        System.out.println(string);
    }

    @Test
    public void promiseThen() {
        //
    }
}*/
